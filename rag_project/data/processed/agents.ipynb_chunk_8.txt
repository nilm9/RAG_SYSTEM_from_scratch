t language models that you can use interchangably  select the one you want to use below import ChatModelTabs from themeChatModelTabs ChatModelTabs openaiParamsmodelgpt4  You can call the language model by passing in a list of messages By default the response is a content string We can now see what it is like to enable this model to do tool calling In order to enable that we use bind_tools to give the language model knowledge of these tools We can now call the model Lets first call it with a normal message a