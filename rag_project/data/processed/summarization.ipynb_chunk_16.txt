lows for individual steps such as successive summarizations to be streamed allowing for greater control of execution  LangGraphs checkpointinghttpslangchainaigithubiolanggraphhowtospersistence supports error recovery extending with humanintheloop workflows and easier incorporation into conversational applications  The LangGraph implementation is straightforward to modify and extend as we will see below  Map Lets first define the prompt associated with the map step We can use the same summarization prompt as