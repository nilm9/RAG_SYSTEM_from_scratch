 as we will see below  Map Lets first define the prompt associated with the map step We can use the same summarization prompt as in the stuff approach above We can also use the Prompt Hub to store and fetch prompts This will work with your LangSmith API keyhttpsdocssmithlangchaincom For example see the map prompt herehttpssmithlangchaincomhubrlmmapprompt python from langchain import hub map_prompt  hubpullrlmmapprompt   Reduce We also define a prompt that takes the document mapping results and reduces them 