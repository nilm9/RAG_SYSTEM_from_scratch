port multiple conversation threads with a single application a common requirement when your application has multiple users We can then invoke the application Great Our chatbot now remembers things about us If we change the config to reference a different thread_id we can see that it starts the conversation fresh However we can always go back to the original conversation since we are persisting it in a database This is how we can support a chatbot having conversations with many users tip For async support up