pers for managing a list of messagesdocshow_tomessages In this case well use the trim_messagesdocshow_totrim_messages helper to reduce how many messages were sending to the model The trimmer allows us to specify how many tokens we want to keep along with other parameters like if we want to always keep the system message and whether to allow partial messages To use it in our chain we just need to run the trimmer before we pass the messages input to our prompt Now if we try asking the model our name it wont k