low us to ask a question about the data in a database and get back a natural language answer The main difference between the two is that our agent can query the database in a loop as many times as it needs to answer the question   Security note  Building QA systems of SQL databases requires executing modelgenerated SQL queries There are inherent risks in doing this Make sure that your database connection permissions are always scoped as narrowly as possible for your chainagents needs This will mitigate thou