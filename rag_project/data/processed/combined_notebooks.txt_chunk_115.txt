re using a high level interface to construct the agent but the nice thing about LangGraph is that this highlevel interface is backed by a lowlevel highly controllable API in case you want to modify the agent logic Now we can initialize the agent with the LLM and the tools Note that we are passing in the model not model_with_tools That is because create_react_agent will call bind_tools for us under the hood from langgraphprebuilt import create_react_agent agent_executor  create_react_agentmodel tools  Run th