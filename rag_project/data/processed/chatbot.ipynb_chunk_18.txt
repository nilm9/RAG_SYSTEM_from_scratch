king messages as input Next well add in more input besides just the messages To add in a system message we will create a ChatPromptTemplate We will utilize MessagesPlaceholder to pass all the messages in We can now update our application to incorporate this template We invoke the application in the same way Awesome Lets now make our prompt a little bit more complicated Lets assume that the prompt template now looks something like this Note that we have added a new language input to the prompt Our applicatio