 message The input to this prompt template is a dictionary We can play around with this prompt template by itself to see what it does by itself prompt  prompt_templateinvokelanguage Italian text hi prompt We can see that it returns a ChatPromptValue that consists of two messages If we want to access the messages directly we do promptto_messages Finally we can invoke the chat model on the formatted prompt response  modelinvokeprompt printresponsecontent tip Message content can contain both text and content b