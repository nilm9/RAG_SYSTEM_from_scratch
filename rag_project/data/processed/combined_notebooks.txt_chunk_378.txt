ntmemory import MemorySaver memory  MemorySaver graph  graph_buildercompilecheckpointermemory interrupt_beforeexecute_query  Now that were using persistence we need to specify a thread ID  so that we can continue the run after review config  configurable thread_id 1 displayImagegraphget_graphdraw_mermaid_png Lets repeat the same run adding in a simple yesno approval step for step in graphstream question How many employees are there config stream_modeupdates  printstep try user_approval  inputDo you want to 