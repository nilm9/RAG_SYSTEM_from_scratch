inaigithubiolanggraphconceptspersistence for more detail including how to use different persistence backends eg SQLite or Postgres We now need to create a config that we pass into the runnable every time This config contains information that is not part of the input directly but is still useful In this case we want to include a thread_id This should look like This enables us to support multiple conversation threads with a single application a common requirement when your application has multiple users We ca