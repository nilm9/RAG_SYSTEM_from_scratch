olved schema information you can use enhanced_schema option The enhanced_schema option enriches property information by including details such as minimum and maximum values for floats and dates as well as example values for string properties This additional context helps guide the LLM toward generating more accurate and effective queries Great Weve got a graph database that we can query Now lets try hooking it up to an LLM  GraphQACypherChain Lets use a simple outofthebox chain that takes a question turns i