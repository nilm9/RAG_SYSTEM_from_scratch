now the answer important For best performance document the schema well and make sure the model isnt force to return results if theres no information to be extracted in the text   The Extractor Lets create an information extractor using the schema we defined above from typing import Optional from langchain_coreprompts import ChatPromptTemplate MessagesPlaceholder from pydantic import BaseModel Field  Define a custom prompt to provide instructions and any additional context  1 You can add examples into the pr