sues and extract the properties used for filtering For string properties we validate them against the database using a simple CONTAINS clause Based on the validation results the process can take the following paths  If value mapping fails we end the conversation and inform the user that we couldnt identify a specific property value eg a person or movie title  If errors are found we route the query for correction  If no issues are detected we proceed to the Cypher execution step from neo4jexceptions import C