ing Messages Weve seen how the agent can be called with invoke to get a final response If the agent executes multiple steps this may take a while To show intermediate progress we can stream back messages as they occur  Streaming tokens In addition to streaming back messages it is also useful to stream back tokens We can do this with the astream_events method important This astream_events method only works with Python 311 or higher   Adding in memory As mentioned earlier this agent is stateless This means it