contenthi responsecontent We can now see what it is like to enable this model to do tool calling In order to enable that we use bind_tools to give the language model knowledge of these tools model_with_tools  modelbind_toolstools We can now call the model Lets first call it with a normal message and see how it responds We can look at both the content field as well as the tool_calls field response  model_with_toolsinvokeHumanMessagecontentHi printfContentString responsecontent printfToolCalls responsetool_ca